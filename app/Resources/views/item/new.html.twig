{% extends 'base.html.twig' %}

{% block body %}
<h1>Adding a new picture</h1>

{{ form_start(form) }}
    {{ form_row(form.spot) }}
    {{ form_row(form.url) }}
    {{ form_row(form.categories) }}
    <input type="submit" value="Create" />
{{ form_end(form) }}

{% endblock %}
