{% extends 'base.html.twig' %}

{% block body %}
  <div class="container">
    <ul>
      <li>
        <a href="{{ path('spot_index') }}"><i class="material-icons" style="vertical-align: middle; font-size: 30px;">view_list</i> Liste des spots</a>
      </li>
    </ul>
    <h4>Mettre à jour le spot</h4>
    <h1 class="center-align">{{spot.title}}</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div style="text-align: right; margin-top:20px;">
          <button class="btn waves-effect waves-light" type="submit" name="action" value="Edit">Editer
            <i class="material-icons right">send</i>
          </button>
        </div>
    {{ form_end(edit_form) }}

    <!-- <ul>
        <li>
            <a href="{{ path('spot_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul> -->
  </div>
{% endblock %}
